{
  "name": "jonasrosland-blog",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "hugo --minify",
    "generate-resume-pdf": "node scripts/generate-resume-pdf.mjs",
    "build:site": "npm run build && npm run generate-resume-pdf"
  },
  "devDependencies": {
    "playwright": "^1.51.0"
  },
  "engines": {
    "node": ">=22"
  }
}
